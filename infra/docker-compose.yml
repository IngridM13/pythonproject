version: '3.9'

services:
  db_postgres:
    image: postgres:15-alpine
    ports:
      - "5433:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=uapp
      - POSTGRES_PASSWORD=papp
      - POSTGRES_DB=postgres
